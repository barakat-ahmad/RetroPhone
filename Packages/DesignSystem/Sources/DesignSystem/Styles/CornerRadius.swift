import CoreFoundation

// MARK: - CornerRadius

/// An enum representing predefined corner radius values.
public enum CornerRadius {
    /// 4px
    public static let s: CGFloat = 4

    /// 8px
    public static let m: CGFloat = 8

    /// 16px
    public static let l: CGFloat = 16
}
